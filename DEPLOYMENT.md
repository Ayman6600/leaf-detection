# Cloudflare Pages Deployment

## Build Configuration
- Build command: `npm run build`
- Build output directory: `dist`
- Root directory: `/` (handled by root package.json)

## Deployment Status
Automatically deploys from the `main` branch on every push.

Domain: leaf-detection.pages.dev
